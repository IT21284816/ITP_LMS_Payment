import React from 'react'

export default function Success() {
  return (
    <div>
        <center>
        <button onClick={() => window.location.href = "http://localhost:3001/pay"}>PAY Now</button>

        </center>
    </div>
  )
}
